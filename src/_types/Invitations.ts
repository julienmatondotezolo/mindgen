export type Invitations = {
  createdAt: string;
  expiredAt: string;
  id: string;
  inviteeId: string;
  inviteeUsername: string;
  inviterId: string;
  inviterUsername: string;
  role: string;
  status: string;
};
